#include "Window.h"
#include "Core/Application.h"
namespace BeeEngine{

    /*Window &Window::Create(const String &title, uint16_t width, uint16_t height)
    {
        switch (Application::GetOsPlatform())
        {
            case OSPlatform::Windows:
                return ;
            case OSPlatform::Linux:
                return ;
            case OSPlatform::Mac:
                return ;
            case OSPlatform::iOS:
                return ;
            case OSPlatform::Android:
                return ;
        }
    }*/
}